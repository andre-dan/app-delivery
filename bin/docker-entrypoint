#!/bin/bash -e

# Se o comando for para iniciar o servidor Rails
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  echo "==> Preparando o banco de dados..."
  ./bin/rails db:prepare
  
  echo "==> Rodando seeds..."
  ./bin/rails db:seed
fi

exec "${@}"